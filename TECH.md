[# 프로젝트 기술서: ArUco ID 기반 Pick and Place

> **작성 상태: 초안 (2026-08-10 기준, 계속 업데이트 예정)**

## 1. 프로젝트 개요

**프로젝트명** : ArUco ID 기반 Pick and Place
**수행 기간** : 2026.08.10~14

### 팀원 및 담당 역할

* **이동헌**: PM, 코드 통합, 문서 작업
* **주동건**: 아루코 마커 생성 및 인지
* **이서현**: 대시보드 제작
* **이호영**: 매니퓰레이터 이동

---

## 2. 사용 기술 스택

| 구분 | 사용 기술 |
| --- | --- |
| **로봇 미들웨어** | ROS2 (Jazzy), rclpy |
| **시뮬레이터** | Gazebo (gz sim), gz_ros2_control (position 인터페이스) |
| **로봇 팔** | OpenManipulator-X (4자유도 팔 + 그리퍼) |
| **컴퓨터 비전** | OpenCV `cv2.aruco` (DICT_4X4_50), 자체 pinhole 카메라 모델 기반 world 좌표 변환 |
| **웹 대시보드** | Flask (Python), MJPEG 스트리밍, HTML/CSS/JS(바닐라) |
| **로봇-웹 연동** | 전용 스레드 + `queue.Queue` 기반 명령 전달 |
| **동작 계획(일부)** | MoveIt2(MoveItPy) — 보조 실행 경로 |

---

## 3. 세부 수행 목표 및 주요 기능

### 3.1. 수행 목표

* 여러 개의 ArUco 마커 박스를 지정한 자리(slot)로 정확하게 옮기기
* 박스의 현재 위치를 카메라가 실시간으로 스스로 인식하기
* 이미 다른 박스가 있는 자리로는 이동하지 못하도록 방지
* 웹 브라우저에서 실시간 카메라 화면을 보며 조작 가능하게 만들기

### 3.2. 주요 기능

* **ArUco ID 기반 박스 인식**: 박스 6면 전체에 동일한 마커를 부착해 어느 면이 위를 향해도 인식 가능
* **자리(slot) 점유 감지**: 천장 고정 카메라가 실시간으로 점유 상태 갱신 (디바운스 적용)
* **마커 ID 기반 이동 명령**: 출발 위치는 시스템이 카메라로 스스로 찾음
* **안전한 이동 경로**: 항상 안전 높이로 들어올린 뒤 회전 → 하강, 충돌/낙하 방지
* **웹 대시보드**: 실시간 카메라 스트리밍 + 상태 모니터링 + 수동 조작(TEACH) + 이동 명령(COMMAND)

---

## 4. 시스템 및 하드웨어 구성

### 4.1. 시스템 아키텍처

* **`BoxSortProject`(ROS2 노드)**: 카메라 인식, 자리 점유 계산, 관절/그리퍼 제어, TEACH/COMMAND 실행을 모두 담당
* **`web_control.py`(Flask 앱)**: `BoxSortProject`를 재사용. "로봇 담당 스레드" 1개가 `rclpy.spin_once()`를 계속 돌리며 큐의 명령을 순차 처리하고, Flask는 별도 스레드에서 HTTP 요청을 큐에 넣고 결과를 기다림 — rclpy와 Flask 이벤트 루프가 서로 안 막히게 분리
* **동기화 방식**: 웹 요청 → 큐에 (함수, 인자, 완료 Event, 결과) 전달 → 로봇 스레드가 순서대로 실행 → Event set → 응답 반환. 카메라는 최신 프레임을 노드 속성에 저장해두고 스트리밍 엔드포인트가 읽어감.

### 4.2. 하드웨어 구성 (시뮬레이션 기준)

* **OpenManipulator-X**: 4자유도(joint1~4) + 평행 그리퍼. joint1은 ±180도 하드웨어 한계.
* **손목 카메라(eye-in-hand)**: `link5` 고정, 시야각 60도, 640x480.
* **천장 고정 카메라(eye-to-hand)**: world (0,0,0.8) 수직 아래, 시야각 80도, 1280x960.
* **ArUco 박스**: 5cm 정육면체, 6면 전체 동일 ID 마커 부착.

---

## 5. 통신 규격

### 5.1. 데이터 송수신 스키마

* **ROS2 내부**: `/joint_states`, `/arm_controller/follow_joint_trajectory`(액션), `/gripper_controller/gripper_cmd`(액션), `/gripper_camera/image_raw`, `/overhead_camera/image_raw`
* **웹 ↔ 서버**: HTTP + JSON(REST), 카메라는 MJPEG 스트리밍

**`GET /status` 응답 예시**:
```json
{
  "joint_position": [0.7854, 0.85, 0.05, -0.85],
  "slot_occupancy": {"1": 0, "3": 1},
  "teach_cursor": 1,
  "total_slots": 4
}
```

**`POST /move` 요청 예시**:
```json
{"marker_id": 0, "to_slot": 3}
```

---

## 6. Backend API 명세

| Method | Path | 역할 |
| --- | --- | --- |
| GET | `/` | 대시보드 HTML 페이지 반환 |
| GET | `/status` | 관절 각도, 자리 점유 현황, TEACH 커서 등을 JSON으로 반환 |
| POST | `/jog` | 키보드 조작과 동일한 동작 1회 실행 (관절 이동, 그리퍼, 자리 커서, 자리 저장) |
| POST | `/move` | 지정한 마커 ID를 지정한 자리로 이동 (출발 자리는 자동 판단) |
| GET | `/video/overhead` | 천장 카메라 실시간 스트리밍 (자리 번호 라벨 포함) |
| GET | `/video/gripper` | 손목 카메라 실시간 스트리밍 |

---

## 7. 화면 구성 (UI/UX)

* **카메라 영역**: 천장/손목 카메라 실시간 화면 (천장 카메라엔 자리 번호 라벨 오버레이 — 빈 자리=하늘색, 점유=주황색)
* **지금 상태 영역**: 관절 각도, TEACH 커서, 자리별 점유 현황 (1초 주기 자동 갱신)
* **수동 조작(TEACH) 영역**: 관절 8방향 버튼, 그리퍼 열기/닫기, 자리 커서 이동, 자리 저장 버튼
* **박스 이동(COMMAND) 영역**: 마커 ID / 도착 자리 드롭다운 + 이동 버튼

---

## 8. 핵심 코드 로직 및 데이터 플로우

### 8.1. 핵심 모듈 연산식 및 기능

* **마커 위치 → 자리 판정**: ArUco tvec(카메라 광학좌표계)을 `world_x = -tvec.y`, `world_y = -tvec.x`로 변환(천장 카메라가 원점 바로 위 수직 하방을 보는 기하학적 관계 이용). 각 자리는 반지름 `SLOT_RADIUS_M`, 각도는 joint1 값으로 정의된 원 위 좌표라, 유클리드 거리가 가장 가까운 자리를 매칭.
* **점유 디바운스**: "생기면 즉시 반영, 없어지면 N프레임 연속 미검출이어야 반영" — 순간 인식 실패로 안전장치가 무력화되는 것 방지.
* **자리 라벨 픽셀 변환**: 위 변환의 역방향 + 핀홀 카메라 모델로 화면 픽셀 좌표 계산, 박스 유무와 무관하게 자리 위치 항상 표시.
* **안전 이동 경로**: (현재 각도에서 안전 높이로 상승) → (상승 상태로 회전) → (목표 각도에서 하강) 3단계로, 충돌·낙하 방지.
* **거리 비례 이동 속도**: 이동 시간 = 관절 거리 / 최대 각속도로 계산해 회전각이 커져도 항상 안전한 속도 유지.

### 8.2. 시스템 운영 사이클

1. **인식 사이클**: 프레임 수신 → 마커 검출 → world 좌표 계산 → 자리 판정 → 점유 갱신(디바운스) → 오버레이 → 스트리밍
2. **이동 명령 사이클**: 명령 입력 → 마커 현재 자리 역조회 → 도착 자리 점유 확인 → 그리퍼 열기 → 안전 경로 이동 → 집기 → 안전 경로 이동 → 놓기
3. **웹 대시보드 사이클**: `/status` 1초 폴링, `/video/*` 지속 스트리밍, 버튼 클릭 → `/jog`·`/move` → 큐 → 로봇 스레드 실행 → 응답

---

## 9. 참조

* **GitHub Repository**: https://github.com/britams/ROS_Team1
* **발표 자료**: [추후 입력]
=======
* **[GitHub Repository]**: [링크 또는 출처 입력]
* **[발표 자료]**: [링크 또는 출처 입력]](https://github.com/ACUBCU/ROS_Team1)

